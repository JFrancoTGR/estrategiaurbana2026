<button
  id="home-floating-cta"
  class="home-floating-cta"
  type="button"
  data-motion="home-floating-cta"
  aria-label="Call to action"
>
  <span class="home-floating-cta__label">
    Call to action
  </span>

  <span
    class="home-floating-cta__icon"
    aria-hidden="true"
  >
    +
  </span>
</button>

<style>
  .home-floating-cta {
    position: fixed;
    z-index: 900;

    top: 74dvh;
    right: 0;

    display: inline-flex;
    align-items: center;
    justify-content: flex-end;

    height: 56px;
    min-width: 56px;
    padding: 0;

    font-family: var(--font-primary);
    font-size: 0.75rem;
    font-weight: var(--font-weight-semibold);
    line-height: 1;
    letter-spacing: 0.08em;
    text-transform: uppercase;

    color: var(--color-white);
    background-color: var(--color-red);
    border: 0;

    cursor: pointer;

    opacity: 0;
    visibility: hidden;
    pointer-events: none;

    transform: translateX(14px);

    transition:
      opacity 420ms var(--ease-premium),
      transform 420ms var(--ease-premium),
      visibility 420ms;
  }

  .home-floating-cta.is-visible {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;

    transform: translateX(0);
  }

  .home-floating-cta__icon {
    flex: 0 0 56px;

    display: flex;
    align-items: center;
    justify-content: center;

    width: 56px;
    height: 56px;

    font-size: 1.25rem;
    font-weight: var(--font-weight-light);
    line-height: 1;
  }

  .home-floating-cta__label {
    overflow: hidden;

    max-width: 0;

    opacity: 0;

    white-space: nowrap;

    transform: translateX(8px);

    transition:
      max-width 420ms var(--ease-premium),
      opacity 260ms var(--ease-premium),
      transform 420ms var(--ease-premium),
      padding-left 420ms var(--ease-premium);
  }

  @media (hover: hover) and (pointer: fine) {
    .home-floating-cta:hover .home-floating-cta__label,
    .home-floating-cta:focus-visible .home-floating-cta__label {
      max-width: 220px;

      padding-left: 24px;

      opacity: 1;

      transform: translateX(0);
    }
  }

  .home-floating-cta:focus-visible {
    outline: 1px solid var(--color-white);
    outline-offset: -3px;
  }

  @media (max-width: 767px) {
    .home-floating-cta {
      top: 42dvh;

      width: 52px;
      min-width: 52px;
      height: 52px;
    }

    .home-floating-cta__icon {
      flex-basis: 52px;

      width: 52px;
      height: 52px;
    }

    .home-floating-cta__label {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .home-floating-cta,
    .home-floating-cta__label {
      transition: none;
    }
  }
</style>